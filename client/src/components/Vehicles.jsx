import React, { useRef, useState } from 'react';
import { Card, Button, Form, Alert } from 'react-bootstrap';

// Create an arrow function to map and display data in a table that allows a user to edit or delete data.

